(function (window) {

    window.__env = window.__env || {};

    window.__env.urlGeoServer = URL_GEOSERVER;

    window.__env.urlViaCEP = URL_VIA_CEP;

    window.__env.urlStacCompose = URL_STAC_COMPOSE;

    window.__env.urlDGIBack = URL_API;

    window.__env.providersToken = PROVIDERS_TOKEN;
    
}(this));